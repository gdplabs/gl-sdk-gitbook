# Modality Transformer

### What's a Modality Transformer

Modality Transformer is a wrapper component that transform an input into other modalities using one or more modality converters. It serves a similar role to a query transformer but is specialized for handling multimodal inputs.

Currently, the component supports this modality transformation:

1. [Image Modality Transformer](image-modality-transformer/)
