# Local vs Remote

GL Runner can run locally for development or remotely for shared environments.
Use the same SDK/CLI surface in both cases; only the base URL and credentials
change.

Local:

- Base URL: `http://localhost:4200`
- Use when running a dev server on your machine

Remote:

- Base URL: your hosted runner URL
- Use when running shared or production environments
